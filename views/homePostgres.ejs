<html lang="en">

    <body>
        <div class=>
          <form action="/home/postgres/" method="GET">
            <label>Search for pets or owners:
              <input type="text" name="phrase" placeholder="PostgreSQL search">
              <button type="submit">PostgreSQL Search</button>
            </label>
          </form>
        </div>
        <!-- <h1>Search Results for "%= searchPhrase %>"</h1>

          <% if (error) { %>
              <p><%= error %></p>
          <% } else if (results.length > 0) { %>
              <ul>
                  <% results.forEach(function(pets) { %>
                      <li><%= pets.pet_name %> - <%= pet_type.name %></li>
                  <% }); %>
                  <% results.forEach(function(owner) { %>
                    <li><%= owner.name %> - <%= owner.owner_id %> - <%= pets.pet_name %></li> 
                <% }); %>
              </ul>
          <% } else { %>
              <p>No results found.</p>
          <% } %> -->
        
    </body>
</html>